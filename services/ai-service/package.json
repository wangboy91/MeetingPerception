{
  "name": "@meeting-copilot/ai-service",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8765",
    "lint": "python -m ruff check app tests",
    "test": "python -m pytest"
  }
}
